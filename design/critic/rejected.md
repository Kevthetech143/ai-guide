# rejected.md — ai-guide-006b round 1

- Cut Popular questions 12 to 6: rejected — questions.json has no popularity signal to choose the 6, and the 12 rows are the internal-link backbone of the 12 SEO pages; they are compact single-line links, not a card wall.

- Round-2 cut-list item (crumb label -> Home) NOT applied as prescribed: built a real /all/ everything page instead, so the 109 crumbs keep the true All AIs label and point at a real list; the escape hatches do too.

- Round-3 cut-list item (drop the Good for fact row): rejected — the row carries multi-category membership (e.g. Chat, Search) shown nowhere else on provider pages; single-category filler accepted as the cost.
